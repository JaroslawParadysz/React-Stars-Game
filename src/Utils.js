const utils = {
    createRange: function() {
        const range = [];
        for(let i = 1; i < 10; i++) {
            range.push(i);
        }
      
        return range;
    }
}

export default utils;